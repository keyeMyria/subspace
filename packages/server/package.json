{
  "name": "@subspace/server",
  "version": "0.0.0",
  "description": "Subspace game server",
  "main": "index.js",
  "scripts": {
    "build": "node ../../lib/build",
    "start":
      "cross-env NODE_ENV=development npm run build && npm run serve",
    "serve": "cross-env NODE_ENV=production node lib/index.js",
    "prepare": "npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@subspace/core": "^0.0.0",
    "@web-udp/server": "^2.0.2",
    "cross-env": "^5.1.4",
    "mongoose": "^5.0.10",
    "redis": "^2.8.0",
    "shortid": "^2.2.8"
  }
}
